{
  "name": "@specodec/typespec-emitter-swift",
  "version": "0.0.28",
  "type": "module",
  "main": "dist/index.js",
  "typespec": {
    "emitter": true,
    "options": {
      "@specodec/typespec-emitter-swift": {
        "type": "object",
        "properties": {
          "emitter-output-dir": {
            "type": "string"
          },
          "ignore-reserved-keywords": {
            "type": "boolean"
          }
        },
        "required": [
          "emitter-output-dir"
        ]
      }
    }
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@specodec/typespec-emitter-core": "^0.0.24"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.11.0",
    "typescript": "^5.8.0"
  }
}
